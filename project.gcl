@include("backend");
@library("std", "7.3.300-stable");
@library("explorer", "7.3.13-stable");

fn main() {
    if (nrCellCU_by_nRCellId.size() == 0) {
        info("Importing Data ...");
        importData();
        info("Import done.");
    }
    info("Application ready to serve.");
}
